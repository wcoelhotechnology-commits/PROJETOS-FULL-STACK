Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1
python-dotenv==1.0.0

# Dependência usada apenas pelos testes automatizados (pasta backend/testes).
# Não é necessária para rodar a aplicação em produção.
pytest==8.3.3
